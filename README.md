# Idea Planner

一个极简黑白风格的桌面应用，包含：
- 番茄时钟计时（专注 + 休息）。
- 每日规划与任务清单（本地保存）。
- 开机自启动开关（Electron 登录项）。
- 一键跳转到 ChatGPT 与 Gemini 的对话入口。

## 开发

```bash
npm install
npm run start
```

## 打包为 Windows .exe

```bash
npm run build:win
```

生成的安装包位于 `dist/` 目录中。
